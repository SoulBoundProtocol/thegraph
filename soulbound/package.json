{
  "name": "soulbound",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ soulbound",
    "create-local": "graph create --node http://localhost:8020/ soulbound",
    "remove-local": "graph remove --node http://localhost:8020/ soulbound",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 soulbound"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.1",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
